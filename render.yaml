services:
  - type: web
    name: trello-backend
    env: python
    plan: starter
    root: backend
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
